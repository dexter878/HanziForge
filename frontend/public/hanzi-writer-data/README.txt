Place local Hanzi Writer JSON files here.
Expected filename format: <character>.json
Example: 你.json

Without these files, Dictionary/Writing pages stay local-only and will not request any CDN,
but stroke-order animation will show a local warning instead.
